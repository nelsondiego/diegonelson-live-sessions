'use client';

const Separator = () => {
  return (
    <div className="w-full max-w-7xl mx-auto px-4 py-12">
      <div className="w-full h-px bg-gradient-to-r from-transparent via-neon-cyan/50 to-transparent opacity-50" />
    </div>
  );
};

export default Separator;